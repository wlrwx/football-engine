# 升级验收报告（2026-08-29）

> 本轮升级 = 融合链重构 + 消融定参 + 投注层闸门（commit `d7f37726`），
> 本报告为其独立验收 + 遗留提升点定位。
> 工具：`scripts/upgrade_acceptance.py`（可重复运行）、`scripts/ledger_deepdive.py`。

## 一、升级前账本诊断（426 场，`ledger_deepdive.py`）

| 发现 | 数据 | 结论 |
|---|---|---|
| 市场强于流水线 | 市场 argmax 命中 54.7% / Brier 0.583，生产 final 50.9% / 0.617 | 概率层一切偏离市场的动作整体为负 |
| 反向下注送命 | 与市场反向 34 场命中 **17.6%**（同向 54.2%） | 偏离市场的选向是最大失血点 |
| 模型本身弱 | 模型 argmax 44.9%，8 月差距还在扩大（51.9% vs 市场 55.6%） | 模型权重应趋零 |
| 选平局是灾难 | 实际平局率 25.8%，系统选平 15 场命中 1 场（6.7%）；市场 argmax 选平 0 场 | 平局不该由 argmax 主动选 |
| 热门区流血 | 1.0-1.5 档 ROI -323、2.0-3.0 档 -318 | 需要投注层止血 |
| 校准偏差 | 预测 0.6 档实际 47.7%（-12pp）、0.7 档 -8.5pp | 过自信集中在 0.6-0.7 |

## 二、升级验收（`upgrade_acceptance.py`，时间切分 val=后 40%）

| 臂 | 全 Brier | 全命中 | val Brier | val 命中 | 配对 t（全/val） |
|---|---|---|---|---|---|
| A 旧配置（升级前） | 0.5876 | 52.8% | 0.5273 | 59.6% | 基线 |
| **B 新包（已提交）** | **0.5847** | **53.8%** | **0.5239** | **59.6%** | **+2.58 / +2.05 ✅** |
| C model=0 权重最优 | 0.5836 | 54.0% | 0.5220 | 60.2% | +2.49 / +2.22 ✅ |
| D 纯市场影子 | 0.5882 | 54.2% | 0.5235 | 60.8% | -0.28 / +1.30 ❌ |

- **B（已提交的新包）双段过 t≥2 显著闸，正式生效**：与市场反向选向 34→5 场；
  Brier 0.617→0.585（重放口径），命中 +0.9pp。
- C（model 权重 0.05→0，market 0.75→0.60/djyy 0.15 归一后 0.8/0.2）优于 B，
  但 C-vs-B 配对 t 全样本 1.53 / 验证段 1.68，**未达 1.96 闸** → 按治理规则不手动翻权重。

## 三、遗留提升点（交给每周优化器，勿手动跳闸）

1. **model→0**：每周优化器（weekly_run 已统一读 config["optimizer"]，model 下界已放开至 0.0）
   会用新增结算样本复评；若 C-vs-B 维持同向，预计 1-2 周内自然达标翻转。
2. **流血联赛观察**（n<50，按治理规则暂不翻转）：瑞典超 33.3%、芬超 32.0%、巴甲 26.3%、
   葡超 52.2% vs 市场 63.2%、解放者杯 25.0%。到 n≥50 仍显著弱于市场 → 进送钱区。
3. **平局选择**：样本太小（重放后 2-3 场），暂不动 combo_boost（消融判定"留"）。
4. **0.6-0.7 档过自信**：model→0 后 final≈市场，市场该档校准良好，预计自然修复。

## 四、本轮改动清单

- `d7f37726`（另一会话）：fusion.py 重构、消融裁决关闭 5 个有害开关
  （lgbm_blend/sina/isotonic/temperature/freshness）、冠军权重 0.10/0.75/0.15→0.05/0.75/0.20、
  水位信号迁至投注层闸门（conflict×0.5）、热门区(<1.8)注量×0.5、weekly_run 配置统一
- 本报告 + `scripts/ledger_deepdive.py` + `scripts/upgrade_acceptance.py`（验收工具，可复跑）

## 五、投注闸门实验（2026-08-29 晚，`scripts/gate_experiment.py`）

以 B 新包为基线，重放三个候选方向闸门 + 一个仓位闸门（同 60/40 时间切分，
0/1 命中差异报配对 t + McNemar 精确检验）：

| 候选 | 全 Δpp | t | val Δpp | t | McNemar p | 结论 |
|---|---|---|---|---|---|---|
| E 反向全让（不一致即跟市场） | +0.5 | 1.00 | +1.2 | 1.42 | .625/.500 | ❌ 未过闸 |
| E2 反向有条件让（前二差<0.05） | +0.2 | 1.00 | +0.6 | 1.00 | 1.0/1.0 | ❌ 未过闸 |
| F 禁选平局（市场不选平时） | +0.2 | 1.00 | +0.6 | 1.00 | 1.0/1.0 | ❌ 未过闸（B 已基本不选平） |
| G 热门区 ×0.5 → ×0 | 确定性核算：n=111，命中 69.4% 低于盈亏平衡，账本 pnl 合计 -323.1；现行 ×0.5 仍贡献 -161.5 | ✅ **上线**（`favorite_band.stake_factor: 0`，零注候选不再出票） |

- E 方向为正（val +1.2pp）但重放后反向仅 5 场、功效不足，与 model→0
  同批交每周优化器复评；账本累积后重跑 `gate_experiment.py` 即可裁决。
- G 为投注层确定性核算（非统计推断），n=111 ≥ 50 治理门槛，与当日
  ×0.5 折量闸同类证据；回滚 = `stake_factor` 改回 0.5。

## 六、外部参照升级（2026-08-29 深夜，RPS + football-data + DC 交叉验证）

### 6.1 RPS 指标入库（`engine/review/post_match.py` + `scripts/rps_report.py`）

胜平负是有序结果，新增 RPS（比 Brier 更贴口径）。账本即时计算（426 场）：

| 来源 | RPS | 结论 |
|---|---|---|
| market | 0.1967 | 天花板 |
| djyy | 0.2080 (n=123) | 单源优于 final |
| final | 0.2115 | 7 月 0.2187 → 8 月 0.2100，在收敛 |
| model | 0.2254 | 最弱环节，与 Brier 诊断一致 |

新账本记录直接落 `rps_final/rps_market/rps_model` 字段；历史由重放工具即时算，不动不可变账本。

### 6.2 football-data.co.uk 基线（`scripts/import_football_data.py`）

20 个联赛、10 赛季、约 5.4 万场真实比分+赔率 → `data/state/football_data_baselines.json`。
**现 LEAGUE_DRAW_ANCHOR 表量级错误坐实**：巴甲真实平局率 0.276 (n=3655) vs 现表 0.46、
美职联 0.249 (n=4402) vs 现表 0.55、芬超 0.250 vs 0.30、葡超 0.239 vs 0.50。

### 6.3 锚定表重放裁决（`scripts/anchor_replay.py`）：不翻表

真实平局率锚（E1/E2）未跑赢现表（全样本 Brier t=-1.79，val 持平）。现表值虽语义
错误（0.55 不可能是平局率）但账本内有效——属循环论证（值即从该账本 n≥5 拟出）。
按治理规则保持现表，锚定值交每周优化器当自由参数；20 年基础率已存档供复评。

### 6.4 DC 交叉验证（`scripts/dc_crossval_penaltyblog.py`，penaltyblog 参照实现）

本地 5.7 万场历史（19448 场入训，2022-07→2026-07-18）拟合参照 DC，
账本 411 场中 123 场队名可对齐，**全部在训练窗结束之后 = 严格样本外**：

| 来源（同 123 场） | Brier | argmax命中 | RPS |
|---|---|---|---|
| **pb-DC xi=0.001（参照）** | **0.5875** | **52.0%** | **0.2032** |
| 生产 model_raw | 0.6101 | 48.8% | 0.2158 |
| 市场 market | 0.5600 | 55.6% | 0.1917 |
| 生产 final | 0.5991 | 52.8% | 0.2081 |

xi 扫描：0.001 最优，静态(0)次之，≥0.0035 明显恶化 → **温和时间衰减
（半衰期约 1.9 年）正确，激进衰减有害**。

**诊断结论**：模型本体弱（argmax 48.8% vs 市场 55.6%）的主因是生产 DC 的
训练方式（缺时间衰减/数据利用不足），而非 DC 结构本身——一个调好 xi 的
标准参照实现在严格样本外就比生产 model_raw 好 3.2pp 命中、-0.023 Brier。

**建议下一步（交每周训练管线，勿手动跳闸）**：生产 DC 重训采用
xi≈0.001 时间衰减加权 + 本地 19k 场数据集扩容，产物走四臂验收闸；
预期 model_raw 拉平参照实现后，final≈(0.05模型+0.75市场+0.20djyy) 的
融合上界随之抬升。样本局限：123 场队名可对齐（大联赛偏置），
扩充 team_aliases 后应重跑。

## 七、周挑战者数据源升级（2026-08-29 深夜，`scripts/dc_challenger_replay.py`）

深诊发现：生产 model_raw 由 Elo 驱动的 DC+MC 集成产出，而周挑战者
`fit_shrinkage_dc` 只用 match_history.db 348 场训练（357 队，每队不足 1 场）。
四臂滚动重放（144 场对齐子集、全部晚于训练窗 = 严格样本外；A/D 按周滚动
防泄漏，冷启动日计 1/3）：

| 臂 | 全Brier | 全命中 | val命中 | t(全) | t(val) |
|---|---|---|---|---|---|
| A 旧包（DB 滚动，现状） | 0.6917（差于均匀 0.667） | 42.4% | 43.1% | 基线 | 基线 |
| D 旧数据+decay.001 | 0.6920 | 42.4% | 43.1% | +2.04（更差向） | +1.96 |
| **B 新包（csv 2.4万场+decay.001）** | **0.5960** | **52.1%** | **56.9%** | **-2.48 ✓** | -1.25（方向对，欠功效） |
| 生产 model_raw（同子集参照） | 0.6084 | 50.0% | — | — | — |

**决定性因素是数据扩容**（单动衰减参数无效果）。**已落地（不改生产下注行为，
仅升级周挑战者信息质量）**：
- `weekly_run.py`：训练数据 = matches.csv（canon 归一）+ DB 增量；decay 0.0025→0.001；
  滚动评估改按 ISO 周重拟合（csv 逐日拟合会拖垮每周 CI）；预测端队名同步归一
- `team_aliases.py`：新增 `CSV_NAME_ALIAS`（6 条人工确认映射）+ `canon_csv_team()`；
  模糊匹配候选含高危误配（新英格兰 Revolution ≠ 英格兰），未确认不入表

**如实披露**：B 的 val 段 t=-1.25（n=58）未过 1.96 严格闸，全样本显著 + 同子集
优于生产 model_raw；144/411 对齐率偏低（K1/日职/杯赛译名体系不同，待扩别名表）；
CI 成本 +约 35 分钟/周。challenger 若要在产线替换 model_raw，仍须走
champion/challenger 闸（weekly_backtest.json verdict）。
